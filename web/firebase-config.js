// Fill this file from Firebase Console before deploying authenticated production.
// Firebase web config is not a database secret, but keep service accounts and deploy tokens out of this repo.
window.JLPT_FIREBASE_CONFIG = null;
window.JLPT_REQUIRE_GOOGLE_SIGN_IN = false;
window.JLPT_FIREBASE_SDK_BASE = "https://www.gstatic.com/firebasejs/10.12.5";

// Optional local emulator settings:
// window.JLPT_FIREBASE_EMULATORS = {
//   auth: "http://127.0.0.1:9099",
//   firestore: { host: "127.0.0.1", port: 8080 }
// };
